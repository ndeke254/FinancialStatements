% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_classify.R
\name{engine_classify}
\alias{engine_classify}
\title{Classify a PDF page as "native" or "scanned" (FR-EX-01)}
\description{
Uses \code{pdftools::pdf_data()} to retrieve word-level bounding boxes.
A page is \strong{native} when its text layer is dense enough to reconstruct
a table geometrically.  It is \strong{scanned} when the text layer is absent or
too sparse (e.g. a JPEG-embedded scan with no embedded text, as in ABSA).
}
\details{
Classification is \strong{per page} (PRD §7.2) — a multi-page range may contain
a mix; the caller must classify each page independently.
}
