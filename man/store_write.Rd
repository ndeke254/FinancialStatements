% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store_write.R
\name{store_write}
\alias{store_write}
\title{Write long records to the Parquet lake (FR-SV-01, FR-SV-02, FR-SV-03)}
\usage{
store_write(long_dt, lake_dir, pool, edited_by = NA_character_)
}
\arguments{
\item{long_dt}{A \code{data.table} in long/tidy format.  Must contain at
minimum the columns in \code{.LONG_REQUIRED_COLS}.}

\item{lake_dir}{Absolute path to the Parquet lake root.}

\item{pool}{A pool object used for the catalog upsert.}

\item{edited_by}{Username of the admin performing the save.}
}
\value{
Invisibly, the relative \code{lake_path} that was written.
}
\description{
Writes (or overwrites) a slice of the Parquet lake identified by
\verb{(company, statement_type)}.  The overwrite rule from FR-SV-02 is:
\emph{all rows for the matching \verb{(entity, period_end)} in the partition are
replaced}.  Rows for other \verb{(entity, period_end)} combinations in the same
partition are preserved.

FR-SV-03 consistency: the catalog upsert only runs after the Parquet write
succeeds, so a write failure leaves the catalog stale-but-consistent (it
either retains the old path or has no entry yet).

After a successful write, upserts the catalog via \code{\link[=catalog_upsert]{catalog_upsert()}}.
}
