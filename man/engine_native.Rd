% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_native.R
\name{engine_native}
\alias{engine_native}
\title{Native PDF extraction engine (FR-EX-02) and strategy dispatcher}
\description{
The \strong{native engine} calls \code{pdftools::pdf_data()} to obtain word-level
bounding boxes, then passes the tokens to \code{\link[=reconstruct_table]{reconstruct_table()}} for
geometric row/column clustering.  No Java; no \code{tabulapdf}.

The \strong{\code{extract()} dispatcher} (PRD §7.1 strategy interface) selects the
right engine based on the \code{method} argument or — when \code{method} is \code{NULL} —
calls \code{\link[=classify_page]{classify_page()}} to determine it automatically.  The output shape
is identical regardless of which engine runs, so callers never need to
branch on method.
}
