% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_pivot.R
\name{fct_pivot}
\alias{fct_pivot}
\title{Wide ↔ long pivot helpers}
\description{
The store is \strong{long/tidy} (one row per company × entity × period × line
item); the \code{rhandsontable} confirmation grid is \strong{wide} (line items as rows,
one value-column per \code{entity__period_label}).  These two functions bridge
the shapes.

Key design decisions:
\itemize{
\item Column key is \code{entity__period_label} (double-underscore separator).
\item Row key is \verb{(section, line_item, line_item_order, row_role, value_type, unit)}.
\item \code{value_text} (the raw printed string) is stored per column in a parallel
set of \verb{_text__entity__period_label} columns — needed for the grid display
and round-trip audit.
\item Only \code{value_type}, \code{unit}, and \code{currency} are per-line-item in the wide
form (they must be uniform across columns for a given row).
\item Per-column metadata (\code{period_end}, \code{period_type}, \code{audit_status}, \code{entity})
is returned in a separate \code{col_meta} attribute so \code{wide_to_long} can
reconstruct the long form exactly.
}
}
