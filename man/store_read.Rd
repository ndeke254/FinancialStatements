% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store_read.R
\name{store_read}
\alias{store_read}
\title{Read long records from the Parquet lake via DuckDB (FR-AN-02)}
\usage{
store_read(
  pool,
  company,
  statement_type,
  entity = NULL,
  fiscal_year = NULL,
  period_end = NULL,
  audit_status = NULL,
  row_roles = NULL
)
}
\arguments{
\item{pool}{A pool object from \code{\link[=db_pool_connect]{db_pool_connect()}}.}

\item{company}{Company name (maps to Hive partition).}

\item{statement_type}{Statement type (maps to Hive partition).}

\item{entity}{Entity name filter, or \code{NULL} for all.}

\item{fiscal_year}{Integer fiscal year filter, or \code{NULL} for all.}

\item{period_end}{Date filter (exact match), or \code{NULL} for all.}

\item{audit_status}{Audit status filter, or \code{NULL} for all.}

\item{row_roles}{Character vector of \code{row_role} values to include,
or \code{NULL} for all.  Useful for excluding
\code{header}/\code{subtotal}/\code{total} in analytical queries
(FR-AN-04).}
}
\value{
A \code{data.table}, ordered by \code{entity}, \code{period_end},
\code{line_item_order}.
}
\description{
All reads go through the shared pool — the DuckDB \code{v_lake} VIEW handles
partition pruning automatically when \code{company} and \code{statement_type} filters
are supplied.

Queries the \code{v_lake} VIEW with partition and column pruning.  At minimum,
supply \code{company} and \code{statement_type} to target a single partition.
}
