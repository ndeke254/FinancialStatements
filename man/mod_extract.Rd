% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_extract.R
\name{mod_extract}
\alias{mod_extract}
\title{Extract module — region demarcation + native extraction (FR-RG-01..04)}
\description{
Renders the selected page with a rubber-band crop UI (crop_select.js),
assigns one \code{statement_type} per region, then runs the native engine via
\code{\link[=extract]{extract()}} inside an \code{ExtendedTask} (async, FR-EX-05).
}
\details{
For a page range: extraction runs on \code{page_from}; multi-page stitching
(FR-RG-04) is handled by running once per page and rbind-listing the
\code{table_dt} rows with a \code{continuation} flag. (OCR stitching lives in M4.)
}
