% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_reconstruct.R
\name{reconstruct_table}
\alias{reconstruct_table}
\title{Reconstruct a table from word bounding-box tokens}
\usage{
reconstruct_table(tokens, region = NULL, page_width = NULL, method = "native")
}
\arguments{
\item{tokens}{A \code{data.frame} (or \code{data.table}) with columns
\code{x}, \code{y}, \code{width}, \code{height}, \code{text} as returned by
\code{pdftools::pdf_data()[[page]]}.  A \code{space} column is ignored.}

\item{region}{Named list with elements \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}
(all in PDF points), or \code{NULL} for the full page.}

\item{page_width}{Page width in PDF points (\code{pdftools::pdf_info()$pagesize}
or computed from \code{max(tokens$x + tokens$width)}).  Used to determine the
label zone boundary when \code{region} is \code{NULL}.}

\item{method}{Extraction method string to embed in the result
(\code{"native"} or \code{"ocr"}).}
}
\value{
A named list (the "extraction result"):
\describe{
\item{\code{table_dt}}{\code{data.table} with columns \code{line_item_order} (int),
\code{label} (chr), \code{col_1} … \code{col_N} (chr).}
\item{\code{n_value_cols}}{Number of detected value columns.}
\item{\code{col_x_bands}}{Numeric vector of column band X-centres (pts).}
\item{\code{method}}{The method string passed in.}
\item{\code{status}}{\code{"extracted"} on success, \code{"failed"} otherwise.}
}
}
\description{
Reconstruct a table from word bounding-box tokens
}
