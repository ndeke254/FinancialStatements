% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_numbers.R
\name{parse_numbers}
\alias{parse_numbers}
\title{Parse printed cell strings to numeric values (PRD §7.4)}
\usage{
parse_numbers(texts, is_per_share = FALSE, default_unit = "KES'000")
}
\arguments{
\item{texts}{Character vector of raw printed strings.}

\item{is_per_share}{Scalar or logical vector (recycled) — \code{TRUE} for
EPS/DPS rows.}

\item{default_unit}{Scalar or character vector (recycled) — default unit
for currency cells.}
}
\value{
A \code{data.table} with columns \code{value}, \code{value_type}, \code{unit},
\code{value_text} and the same length as \code{texts}.
}
\description{
Converts the raw text in a financial-statement cell to a structured
\verb{(value, value_type, unit)} triple, preserving the original string in
\code{value_text} for audit.  Implements the §7.4 table exactly:

Applies \code{\link[=parse_number]{parse_number()}} to each element and returns a \code{data.table} with
one row per input element.
}
\details{
\tabular{llll}{
   Printed \tab \code{value} \tab \code{value_type} \tab \code{unit} \cr
   \verb{1,234} \tab 1234 \tab currency \tab KES'000 \cr
   \verb{(10,367,887)} \tab −10367887 \tab currency \tab KES'000 \cr
   \verb{−} or blank \tab \code{NA} \tab \code{NA} \tab \code{NA} \cr
   \verb{13.1\%} \tab 13.1 \tab percent \tab \% \cr
   \code{0.30} (EPS/DPS) \tab 0.30 \tab per_share \tab KES \cr
}
}
