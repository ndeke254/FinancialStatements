% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_ai.R
\name{engine_ai_extract}
\alias{engine_ai_extract}
\title{AI/vision extraction engine — stub for v2 (FR-EX-04)}
\usage{
engine_ai_extract(pdf_path, page, region = NULL)
}
\arguments{
\item{pdf_path}{Path to the PDF file.}

\item{page}{Integer page number (1-based).}

\item{region}{Named list \code{list(xmin, xmax, ymin, ymax)} or \code{NULL}.}
}
\value{
An extraction result list with \code{status = "not_available"}.
}
\description{
In \strong{v2} this will:
\enumerate{
\item Render the PDF page to a raster image.
\item Submit the image to a vision model.
\item Parse the structured JSON response into the standard extraction result.
}

In \strong{v1} this stub is \strong{disabled} and always returns
\code{status = "not_available"}.  The strategy interface in \code{\link[=extract]{extract()}} is
designed so that wiring the real implementation in v2 requires only
filling in this function body — no changes elsewhere (FR-EX-04).
}
