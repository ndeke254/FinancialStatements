% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_ocr.R
\name{engine_ocr_extract}
\alias{engine_ocr_extract}
\title{OCR extraction engine — stub for M4 (FR-EX-03)}
\usage{
engine_ocr_extract(pdf_path, page, region = NULL)
}
\arguments{
\item{pdf_path}{Path to the PDF file.}

\item{page}{Integer page number (1-based).}

\item{region}{Named list \code{list(xmin, xmax, ymin, ymax)} or \code{NULL}.}
}
\value{
An extraction result list with \code{status = "not_available"}.
}
\description{
In \strong{M4} this will:
\enumerate{
\item Render the PDF page to a high-resolution raster with
\code{pdftools::pdf_render_page()} (≥ 300 DPI).
\item Run \code{tesseract::ocr_data()} to obtain word bounding boxes.
\item Feed the result into \code{\link[=reconstruct_table]{reconstruct_table()}} (shared with the native path).
}

In \strong{v1 / M2} this stub returns \code{status = "not_available"} so the
dispatcher can fall back gracefully and the caller can surface an
informative message to the user.

The function signature is identical to \code{\link[=engine_native_extract]{engine_native_extract()}} so the
strategy interface in \code{\link[=extract]{extract()}} requires no branching changes in M4.
}
