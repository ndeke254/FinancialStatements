% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_row_role.R
\name{detect_row_role}
\alias{detect_row_role}
\title{Propose \code{row_role} from label keywords and styling cues (FR-ED-05, PRD §7.5)}
\usage{
detect_row_role(label, value_texts = NULL, has_bold = FALSE, indentation = 0L)
}
\arguments{
\item{label}{The row label string (as extracted from the PDF).}

\item{value_texts}{Character vector of raw value-cell texts for this row
(used to detect ratio rows from \verb{\%} values when the label is ambiguous).
Pass \code{NULL} or \code{character(0)} if unavailable.}

\item{has_bold}{\code{TRUE} if the PDF's styling marks this row as bold
(available for native PDFs via \code{pdftools::pdf_data} font_size heuristic).}

\item{indentation}{Non-negative integer: how many leading spaces/indent
units the label has.  Deeply indented rows are rarely totals/headers.}
}
\value{
A character scalar: one of
\code{"header"}, \code{"line"}, \code{"subtotal"}, \code{"total"}, \code{"ratio"}.
}
\description{
Heuristically classifies each row as one of:
\code{header} | \code{line} | \code{subtotal} | \code{total} | \code{ratio}
}
\details{
The result is a \strong{proposal} — the user confirms or overrides in the edit
grid (FR-ED-05).  Engine logic stays Shiny-free; these are plain functions.

Detection priority (highest wins):
\enumerate{
\item \code{ratio}   — EPS/DPS/per-share/ratio keywords, or row has \verb{\%} values
\item \code{total}   — "total" keyword (standalone or terminal), "net total"
\item \code{subtotal}— "net …", "gross …", "profit …/loss …", "result", "surplus",
"subtotal", etc.
\item \code{header}  — no numeric value present (all value cells are NA/blank/dash),
or label matches known section-heading patterns
\item \code{line}    — default
}
}
